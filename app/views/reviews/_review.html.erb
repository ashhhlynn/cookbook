<h3><%= review.recipe.name %>: <%= review.text %>
By <%= link_to review.user.username, user_path(review.user)%>
<%= review.created_at.strftime("%m-%d-%y") %>
<% if current_user && review.user.id == current_user.id %>
    <%= link_to "Edit", edit_recipe_review_path(r, review) %></h3>
<% end %>
